name: calorie_lens_ai_app
description: "Calorie Lens AI - Your Personal Nutrition Assistant"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0' # SDK'yı 3.8.1 ile uyumlu en geniş aralığa güncelledim

dependencies:
  flutter:
    sdk: flutter

  # 🏗️ State Management & Architecture 
  flutter_bloc: ^7.3.3    
  equatable: ^2.0.7       
  provider: ^6.1.1        

  # Firebase
  firebase_core: ^4.2.1   
  firebase_auth: ^6.1.2  
  cloud_firestore: ^6.1.0
  
  # 🔧 Dependency Injection
  get_it: ^9.0.5          
  injectable: ^2.1.2

  # Percent Indicator for progress bars and circular indicators 
  percent_indicator: ^4.2.5

  # 🌐 Network & Data
  dio: ^5.4.3            
  json_annotation: ^4.9.0 
  dartz: ^0.10.1          

  # 💾 Local Storage
  shared_preferences: ^2.2.2 
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Utils (Önceki sorundan dolayı yorum satırından çıkarıldı ve güncellendi)
 # connectivity_plus: ^7.0.0 # Güncel
 # internet_connection_checker: ^6.0.1 # Çatışmayı çözmek için güncellendi

  # Environment
  flutter_dotenv: ^6.0.0  # Güncel

  # File Path Manipulation
  path: ^1.9.1            # Güncel

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Güncel

  # Testing and Code Generation (ÇATIŞMAYI ÇÖZMEK İÇİN GÜNCELLENDİ)
  mockito: ^5.4.4         
  bloc_test: ^8.5.0       
  build_runner: ^2.4.9   
  injectable_generator: ^2.1.2 
  json_serializable: ^6.7.1
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/.env
